---
export interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<section
  class=`min-h-[calc(100vh-0px)] flex items-center justify-center text-foreground overflow-hidden pt-16 py-8 ${className}`
>
  <slot />
</section>